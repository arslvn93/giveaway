// Environment configuration template
// Copy this file to env.js and replace with your actual credentials
const ENV = {
    // Authentication for webhook endpoint
    // Generate a secure random token and add it here
    WEBHOOK_AUTH_TOKEN: "your-secure-token-here",
    
    // Alternative: If you prefer Basic Auth, uncomment and use these:
    // For Basic Auth, you would need to modify the code to use:
    // btoa(`${ENV.WEBHOOK_AUTH_USERNAME}:${ENV.WEBHOOK_AUTH_PASSWORD}`)
    // WEBHOOK_AUTH_USERNAME: "your-username",
    // WEBHOOK_AUTH_PASSWORD: "your-password"
};

// SECURITY NOTES:
// 1. Never commit env.js with real credentials to version control
// 2. Make sure env.js is in your .gitignore file
// 3. Use a strong, randomly generated token (32+ characters recommended)
// 4. Rotate tokens periodically for better security
// 5. Configure your n8n webhook to validate this token on the server side

