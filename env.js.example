// Environment configuration template
// NOTE: Submission authentication is now handled securely via Netlify Functions
// See NETLIFY_SETUP.md for how to configure environment variables

const ENV = {
    // Add any client-side configuration here if needed
    // Do NOT add authentication credentials here - they should be in Netlify env vars
};

// SECURITY NOTES:
// ✅ Authentication is now SERVER-SIDE via Netlify Functions
// ✅ Credentials are stored in Netlify environment variables
// ✅ Credentials are never exposed to the browser
// ✅ See NETLIFY_SETUP.md for setup instructions

